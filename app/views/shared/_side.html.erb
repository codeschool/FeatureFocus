<% if current_user || has_referrer? %>
  <div class='referrer-info'>
    <% if current_user %>
      <div> Logged in as <%= current_user.email %> </div>
    <% end %>

    <% if has_referrer? %>
      <div> Referred by <strong><%= @referrer %></strong> </div>
    <% end %>
  </div>
<% end %>
